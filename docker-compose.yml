services:
  mus-swiss:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      DATA_DIR: /app/data
    volumes:
      - mus_swiss_data:/app/data
    restart: unless-stopped

volumes:
  mus_swiss_data:

